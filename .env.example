DATABASE_URL="postgresql://dnd_booker:dnd_booker_dev@localhost:5432/dnd_booker?schema=public"
REDIS_HOST=localhost
REDIS_PORT=6379
JWT_SECRET=change-me-in-production
JWT_REFRESH_SECRET=change-me-in-production-too
PORT=4000
CLIENT_URL=http://localhost:3000
AI_KEY_ENCRYPTION_SECRET=generate-with-openssl-rand-hex-32-must-be-64-hex-chars

# Worker settings (optional)
# EXPORT_OUTPUT_DIR=/path/to/output  # defaults to ../worker/output
# PUPPETEER_SANDBOX=true  # set to true when NOT running in Docker

# Text layout rollout (optional)
# TEXT_LAYOUT_ENGINE_MODE=legacy  # legacy | shadow | pretext
# VITE_TEXT_LAYOUT_ENGINE_MODE=legacy
# TEXT_LAYOUT_THEME=gilded-folio
